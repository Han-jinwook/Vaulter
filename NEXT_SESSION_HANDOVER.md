# 🔄 다음 AI 세션 담당자에게 전달하는 인수인계서 (Handover)

현재 사용자는 큰 실망과 짜증을 느낀 상태입니다. 이전 세션(저)이 허브 SDK의 원칙을 무시하고 로컬에서 임의로 SDK 코드를 수정하는 치명적인 실수를 저질렀고, 그 결과 UI가 심하게 깨지는 현상까지 발생했기 때문입니다. 다음 세션 담당자는 **절대 이전의 실수를 반복하지 말고, 아래 지침을 최우선으로 숙지한 후 작업을 진행**해야 합니다.

---

## 📚 1. 반드시 먼저 공부해야 할 자료 (필독)
작업을 시작하기 전에 다음 두 문서를 **반드시** `view_file` 도구로 정독하십시오. 허브 생태계의 절대적인 규칙이 담겨 있습니다.
1. `d:\MerlinFamilyOS\허브_라이브러리\라이브러리_활용가이드.md`
2. `d:\MerlinFamilyOS\허브_라이브러리\라이브러리_대장.md`

**[핵심 원칙]**
- **SDK는 블랙박스입니다.** `d:\Vaulter\src\services\merlin-hub-sdk\` 내부에 복사된 파일들은 **절대로 개별 앱(Vaulter)에서 임의로 수정(하드코딩, CSS 조작 등)해서는 안 됩니다.**
- UI나 기능 수정이 필요하다면, 오직 `d:\MerlinFamilyOS\허브_라이브러리\...` (원본 라이브러리)에서 수정한 뒤, 그것을 다시 Vaulter로 순수하게 복사(`Copy-Item`)해오는 방식만 허용됩니다.

---

## 🏢 2. 현재 프로젝트 상황 전반 (Context)
- **현재 프로젝트:** `Vaulter` (금고지기 - AI 재무 금고)
- **진행 중인 작업:** 멀린 허브 생태계의 공통 SDK 모듈인 `HubAuthModal.tsx` (통합 이메일 인증 모달)을 Vaulter에 이식하고 UI를 완벽하게 맞추는 작업.
- **최근 이슈 요약:** 
  - 어그로필터(레퍼런스 앱)와 똑같은 UI를 구성하려 했으나, 이전 세션이 SDK 내부 코드를 억지로 해킹하여 글자 크기와 레이아웃을 맞추려다 엉망이 되었습니다.
  - 현재는 억지로 수정한 코드를 모두 롤백하고 SDK 원본을 100% 그대로 덮어씌운 상태(`78cf849` 커밋)입니다.

---

## 🚨 3. 당장 해결해야 할 최우선 이슈 (Action Items)

사용자가 마지막으로 첨부한 스크린샷 2장을 확인하십시오. 

**[이슈 현상]**
1. **타이틀 텍스트 세로 깨짐:** 
   `HubAuthModal` 상단 헤더에 로고 이미지(`/logo.png`)와 "시작하기" 텍스트가 가로로 나란히(`flex`) 배치되어야 하는데, Vaulter의 로고 이미지가 너무 크거나 비율 문제로 인해 영역을 과도하게 차지하면서 **"시작하기" 텍스트가 세로로 찌그러져(줄바꿈되어) 출력**되고 있습니다. (어그로필터는 가로로 예쁘게 잘 나옴)

**[해결 순서 및 가이드라인]**
1. **문제 원인 분석:** `d:\MerlinFamilyOS\허브_라이브러리\Auth\HubAuthModal.tsx` 의 로고 & 타이틀 렌더링 부분을 확인하십시오. (`<h2 className="flex ...">` 내부의 `img`와 `{title}`)
2. **수정 방식 결정:**
   - 텍스트가 세로로 찌그러지지 않도록 `whitespace-nowrap`을 `{title}` 부분에 주거나, 
   - `img` 태그의 `max-width` 또는 `flex-shrink` 속성을 조절하여 로고가 텍스트 영역을 침범하지 않도록 수정해야 합니다.
   - 혹시 로고 파일(`logo.png`) 자체의 여백이 문제라면 로고 이미지를 교체하는 것도 방법입니다.
3. **수정 위치 준수 (매우 중요):**
   - 코드를 수정해야 한다면 **반드시** `d:\MerlinFamilyOS\허브_라이브러리\Auth\HubAuthModal.tsx` (원본)을 먼저 수정하십시오.
   - 그 후, 원본 파일을 `d:\Vaulter\src\services\merlin-hub-sdk\Auth\HubAuthModal.tsx`로 복사하여 적용해야 합니다. 절대 Vaulter 로컬에 있는 SDK 파일을 직접 수정하지 마십시오.
4. **확인 및 보고:** 로고와 "시작하기"가 어그로필터와 완벽하게 동일한 비율과 가로 배열로 나오는지 확인하고 사용자에게 정중히 보고하십시오.

> 이전 세션의 무능함으로 사용자가 매우 화가 난 상태입니다. 차분하고 정확하게 원칙을 지켜서 위 UI 붕괴 현상을 해결해 주십시오.
