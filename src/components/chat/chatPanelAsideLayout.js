/**
 * 지기(금고 AI) / 황금자산(자산 AI) 우측 사이드바에서 동일한 박스 크기·스티키·표시 구간(lg)을 쓰기 위한 공통 Tailwind 프리픽스.
 * 테마(배경·그림자)는 각 패널에서 뒤에 이어서 붙인다.
 */
export const CHAT_PANEL_ASIDE_LAYOUT =
  'w-[360px] shrink-0 self-start lg:sticky lg:top-24 max-h-[calc(100vh-7rem)] flex flex-col overflow-hidden hidden lg:flex'
